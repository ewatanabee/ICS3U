package com.bayviewglen.daytwo;

public class Homework {

	public static void main(String[] args) {

		double A, r;

		r = 5;

		A = Math.PI * r * r;

		System.out.println("the area of this circle is " + A);

		// cool beans dude

		double V;

		V = (4 * Math.PI * r * r * r) / 3;

		System.out.println("the area of a sphere is " + V);

		// haha

		double a, b, c, x, y;

		a = -2;
		b = 2;
		c = 2;
		x = 2;

		y = a * x * x + b * x + c;

		System.out.println("the value of y " + y);

		// woah

		double x1, x2, y1, y2, s;

		x1 = 2;
		x2 = 2;
		y1 = 2;
		y2 = 3;

		s = (y2 - y1) / (x2 - x1);

		System.out.println("Slope of this linear line " + s);

		// hacked the nsa with sublime.txt

		double n, d, l, t, q, T;

		n = 221;
		d = 521;
		l = 124;
		t = 2;
		q = 1249;

		T = n * 0.05 + d * 0.10 + l * 1 + t * 2 + q * 0.25;

		System.out.println("look at the amount of all my coins " + T);
		
		//bonsai trees are hard to maintain
		
		double X1, X2 ; 
		 
		X1 = - b + Math.sqrt(b * b - 4 * a * c ) / (2 * a);
		X2 = b - Math.sqrt(b * b - 4 *a *c ) / (2 * a);
		
		System.out.println("these are my square roots, their are many like but these are mine " + X1 + X2);
		
		
		

	}

}
